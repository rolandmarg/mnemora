# Auto-fix lint issues
echo "🔧 Running lint:fix..."
yarn lint:fix

# Stage any files that were auto-fixed
echo "📝 Staging auto-fixed files..."
git add -u

# Type check (can't be auto-fixed)
echo "🔍 Running type-check..."
yarn type-check
