# Run lint-staged (only checks changed files) - FAST
echo "🔧 Running lint-staged on changed files..."
yarn lint-staged

# Type-check is skipped locally for speed
# Type-check runs in CI/CD to catch errors before merge
